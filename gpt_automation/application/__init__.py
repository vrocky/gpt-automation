"""
Application layer — use cases that orchestrate domain and infrastructure.

Each use case is a user story:
- InitializeProject: "Initialize a new project with .gpt/ directory"
- GeneratePrompts: "Generate directory/content prompts"

Use cases coordinate:
- Domain logic (what to do)
- Infrastructure (how to do it)
- Application flow (in what order)
"""
